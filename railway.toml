[build]
buildCommand = "npm install && npx prisma generate"

[deploy]
startCommand = "npm run build && npx prisma db push --accept-data-loss && node dist/app.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
